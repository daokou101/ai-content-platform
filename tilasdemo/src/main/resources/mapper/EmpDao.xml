<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pzhu.dao.EmpDao">
    <select id="page" resultType="com.pzhu.pojo.Emp">
        SELECT emp.*, dept.name dept_name
        FROM emp
        LEFT JOIN dept ON emp.dept_id = dept.id
        WHERE 1=1
        <if test="name != null and name != ''">
            AND emp.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="gender != null">
            AND emp.gender = #{gender}
        </if>
        <if test="begin != null">
            AND emp.entry_date >= #{begin}
        </if>
        <if test="end != null">
            AND emp.entry_date <![CDATA[ <= ]]> #{end}
        </if>
        ORDER BY emp.update_time DESC
    </select>
    <select id="countByJob" resultType="map">
        SELECT job, COUNT(*) AS count
        FROM emp
        GROUP BY job
        ORDER BY count ASC
    </select>
</mapper>
